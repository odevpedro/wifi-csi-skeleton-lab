simulator:
  roomId: ${ROOM_ID:bedroom}
  deviceId: ${DEVICE_ID:esp32-sim-01}
  frequencyHz: ${FREQUENCY_HZ:20}
  scenario: ${SCENARIO:person_walking}
  endpoint: ${BACKEND_URL:http://localhost:8080}/api/csi/samples
